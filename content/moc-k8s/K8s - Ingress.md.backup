#kubernetes #resource #networking

클러스터 **외부**의 HTTP/HTTPS 요청을 클러스터 **내부**의 `[[K8s - Service]]`로 라우팅하는 규칙을 정의합니다.

*   **역할**: 외부 트래픽을 도메인/경로 기반으로 라우팅
*   **비유**:  아파트 동 입구의 도메인 기반 안내판 (예: `a.com/path1` -> `A 서비스`로, `b.com` -> `B 서비스`로 안내)

---

**작성일**: 2025-11-03
