
## 📋 프로젝트 메타 정보

| 항목 | 내용 |
|------|------|
| **프로젝트명** | 뽀시레기 (BBOSSIREGI) |
| **기간** | 2026.02.24 ~ 2026.03.19 (24일) |
| **팀원** | 4명 |
| **스프린트** | 1주 단위 (총 4스프린트) |

### 우선순위 정의

| 레벨 | 설명 | 기준 |
|------|------|------|
| **P0** | 필수 (Blocker) | 이거 없으면 다음 작업 불가 |
| **P1** | 핵심 (Must Have) | MVP 필수 기능 |
| **P2** | 중요 (Should Have) | 있으면 좋음 |
| **P3** | 선택 (Nice to Have) | 시간 되면 |

---

## 🗓️ Sprint 1: 인프라 기반 구축 (Day 1-5)

### Day 1 (2/24 월) - 설계 및 AWS 기반

| ID | 우선순위 | 태스크 | 담당 | 산출물 | 완료 기준 |
|----|----------|--------|------|--------|-----------|
| `S1-001` | P0 | AWS 계정 및 IAM 설정 | 인프라 | IAM 정책 JSON | Admin, Developer, ReadOnly 역할 생성 완료 |
| `S1-002` | P0 | ERD 설계 확정 | 백엔드 | ERD 다이어그램 | 최소 5개 테이블, 관계 정의 완료 |
| `S1-003` | P0 | 시스템 아키텍처 다이어그램 | 전체 | draw.io 파일 | 모든 컴포넌트 + 데이터 흐름 표시 |
| `S1-004` | P0 | MVP API 명세 작성 | 백엔드 | OpenAPI 3.0 Spec | 최소 10개 엔드포인트 정의 |
| `S1-005` | P0 | Git 레포지토리 구성 | 전체 | GitHub Repo | mono-repo 구조, branch 전략 문서화 |

#### 📝 S1-002: ERD 명세

```sql
-- 필수 테이블 (최소 구현)
users (id, email, password_hash, name, created_at)
products (id, name, description, price, image_url, created_at)
time_deals (id, product_id, deal_price, stock_quantity, reserved_quantity, start_at, end_at, status)
orders (id, user_id, time_deal_id, quantity, total_price, status, created_at)
order_events (id, order_id, event_type, payload, created_at)  -- 이벤트 소싱용
```

#### 📝 S1-004: API 명세 (필수 10개)

```yaml
# 인증 (2개)
POST /api/v1/auth/register     # 회원가입
POST /api/v1/auth/login        # 로그인

# 상품 (3개)
GET  /api/v1/products          # 상품 목록
GET  /api/v1/products/:id      # 상품 상세
POST /api/v1/admin/products    # 상품 등록 (관리자)

# 타임딜 (3개)
GET  /api/v1/timedeals         # 타임딜 목록
GET  /api/v1/timedeals/:id     # 타임딜 상세 (재고 포함)
POST /api/v1/admin/timedeals   # 타임딜 생성 (관리자)

# 주문 (2개)
POST /api/v1/orders            # 주문 생성 (재고 예약)
GET  /api/v1/orders/:id        # 주문 상태 조회
```

---

### Day 2 (2/25 화) - 네트워크 및 DB 구축

| ID | 우선순위 | 태스크 | 담당 | 산출물 | 완료 기준 |
|----|----------|--------|------|--------|-----------|
| `S1-006` | P0 | VPC 생성 | 인프라 | Terraform 코드 | CIDR: 10.0.0.0/16 |
| `S1-007` | P0 | 서브넷 구성 | 인프라 | Terraform 코드 | Public 2개, Private 2개 (Multi-AZ) |
| `S1-008` | P0 | NAT Gateway + IGW | 인프라 | Terraform 코드 | Private 서브넷 인터넷 접근 가능 |
| `S1-009` | P0 | 보안 그룹 설정 | 인프라 | Terraform 코드 | ALB, EKS, RDS 별도 SG |
| `S1-010` | P0 | RDS PostgreSQL 구축 | 인프라 | Terraform 코드 | db.t3.micro, Multi-AZ 비활성화 (비용) |
| `S1-011` | P0 | DB 스키마 생성 | 백엔드 | DDL SQL 파일 | 모든 테이블 + 인덱스 생성 완료 |

#### 📝 S1-006~009: VPC 명세

```hcl
# terraform/vpc.tf
module "vpc" {
  source  = "terraform-aws-modules/vpc/aws"
  version = "5.0.0"

  name = "bbossiregi-vpc"
  cidr = "10.0.0.0/16"

  azs             = ["ap-northeast-2a", "ap-northeast-2c"]
  public_subnets  = ["10.0.1.0/24", "10.0.2.0/24"]
  private_subnets = ["10.0.11.0/24", "10.0.12.0/24"]

  enable_nat_gateway = true
  single_nat_gateway = true  # 비용 절감

  tags = {
    Project     = "bbossiregi"
    Environment = "dev"
  }
}
```

#### 📝 S1-009: 보안 그룹 명세

| SG 이름 | Inbound | Outbound | 용도 |
|---------|---------|----------|------|
| `sg-alb` | 80, 443 from 0.0.0.0/0 | All | ALB |
| `sg-eks` | All from sg-alb | All | EKS 노드 |
| `sg-rds` | 5432 from sg-eks | None | RDS |

#### 📝 S1-011: DDL 명세

```sql
-- migrations/001_init.sql

CREATE TABLE users (
    id SERIAL PRIMARY KEY,
    email VARCHAR(255) UNIQUE NOT NULL,
    password_hash VARCHAR(255) NOT NULL,
    name VARCHAR(100) NOT NULL,
    role VARCHAR(20) DEFAULT 'user',  -- user, admin
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

CREATE TABLE products (
    id SERIAL PRIMARY KEY,
    name VARCHAR(255) NOT NULL,
    description TEXT,
    price DECIMAL(10,2) NOT NULL,
    image_url VARCHAR(500),
    category VARCHAR(50),  -- food, toy, health, etc
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

CREATE TABLE time_deals (
    id SERIAL PRIMARY KEY,
    product_id INTEGER REFERENCES products(id),
    deal_price DECIMAL(10,2) NOT NULL,
    original_price DECIMAL(10,2) NOT NULL,
    stock_quantity INTEGER NOT NULL,
    reserved_quantity INTEGER DEFAULT 0,
    sold_quantity INTEGER DEFAULT 0,
    start_at TIMESTAMP NOT NULL,
    end_at TIMESTAMP NOT NULL,
    status VARCHAR(20) DEFAULT 'scheduled',  -- scheduled, active, ended, soldout
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    
    CONSTRAINT valid_stock CHECK (reserved_quantity >= 0),
    CONSTRAINT valid_sold CHECK (sold_quantity >= 0),
    CONSTRAINT valid_total CHECK (reserved_quantity + sold_quantity <= stock_quantity)
);

CREATE TABLE orders (
    id SERIAL PRIMARY KEY,
    user_id INTEGER REFERENCES users(id),
    time_deal_id INTEGER REFERENCES time_deals(id),
    quantity INTEGER NOT NULL DEFAULT 1,
    unit_price DECIMAL(10,2) NOT NULL,
    total_price DECIMAL(10,2) NOT NULL,
    status VARCHAR(20) DEFAULT 'pending',  -- pending, reserved, confirmed, canceled, failed
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

CREATE TABLE order_events (
    id SERIAL PRIMARY KEY,
    order_id INTEGER REFERENCES orders(id),
    event_type VARCHAR(50) NOT NULL,  -- CREATED, STOCK_RESERVED, PAYMENT_COMPLETED, CONFIRMED, CANCELED
    payload JSONB,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

-- 인덱스
CREATE INDEX idx_time_deals_status ON time_deals(status);
CREATE INDEX idx_time_deals_start_at ON time_deals(start_at);
CREATE INDEX idx_orders_user_id ON orders(user_id);
CREATE INDEX idx_orders_status ON orders(status);
CREATE INDEX idx_order_events_order_id ON order_events(order_id);
```

---

### Day 3 (2/26 수) - 백엔드 핵심 API

| ID | 우선순위 | 태스크 | 담당 | 산출물 | 완료 기준 |
|----|----------|--------|------|--------|-----------|
| `S1-012` | P0 | Go 프로젝트 초기화 | 백엔드 | main.go | Gin 프레임워크 설정 완료 |
| `S1-013` | P0 | DB 연결 설정 | 백엔드 | db/postgres.go | Connection pool 설정 |
| `S1-014` | P1 | 유저 인증 API | 백엔드 | handlers/auth.go | JWT 토큰 발급 |
| `S1-015` | P1 | 상품 CRUD API | 백엔드 | handlers/product.go | 목록/상세/등록 |
| `S1-016` | P1 | 타임딜 API | 백엔드 | handlers/timedeal.go | 목록/상세/등록 |
| `S1-017` | P0 | 타임딜 스케줄링 로직 | 백엔드 | services/scheduler.go | 자동 상태 변경 |

#### 📝 S1-014: 인증 API 명세

```go
// POST /api/v1/auth/register
// Request
{
    "email": "user@example.com",
    "password": "password123",
    "name": "홍길동"
}
// Response 201
{
    "id": 1,
    "email": "user@example.com",
    "name": "홍길동",
    "created_at": "2026-02-26T10:00:00Z"
}
// Error 400: 이메일 중복
// Error 400: 유효성 검사 실패

// POST /api/v1/auth/login
// Request
{
    "email": "user@example.com",
    "password": "password123"
}
// Response 200
{
    "access_token": "eyJhbGciOiJIUzI1NiIs...",
    "token_type": "Bearer",
    "expires_in": 3600
}
// Error 401: 인증 실패
```

#### 📝 S1-016: 타임딜 API 명세

```go
// GET /api/v1/timedeals
// Response 200
{
    "data": [
        {
            "id": 1,
            "product": {
                "id": 1,
                "name": "프리미엄 사료 3kg",
                "image_url": "https://..."
            },
            "deal_price": 29900,
            "original_price": 45000,
            "discount_rate": 33,
            "stock_quantity": 100,
            "available_quantity": 87,  // stock - reserved - sold
            "start_at": "2026-02-27T14:00:00Z",
            "end_at": "2026-02-27T15:00:00Z",
            "status": "scheduled"
        }
    ],
    "meta": {
        "total": 10,
        "page": 1,
        "per_page": 20
    }
}

// GET /api/v1/timedeals/:id
// Response 200 (실시간 재고 포함)
{
    "id": 1,
    "product": { ... },
    "deal_price": 29900,
    "stock_quantity": 100,
    "reserved_quantity": 10,
    "sold_quantity": 3,
    "available_quantity": 87,
    "start_at": "2026-02-27T14:00:00Z",
    "end_at": "2026-02-27T15:00:00Z",
    "status": "active",
    "remaining_seconds": 1823  // 남은 시간 (초)
}
```

#### 📝 S1-017: 스케줄러 로직 명세

```go
// services/scheduler.go

// 매 분마다 실행
func (s *Scheduler) Run() {
    ticker := time.NewTicker(1 * time.Minute)
    for range ticker.C {
        s.activateDeals()   // scheduled → active
        s.expireDeals()     // active → ended (시간 만료)
        s.checkSoldOut()    // active → soldout (재고 소진)
    }
}

// 상태 전이 규칙
// scheduled → active: start_at <= now AND now < end_at
// active → ended: now >= end_at
// active → soldout: available_quantity <= 0
```

---

### Day 4 (2/27 목) - 주문 및 컨테이너화

| ID | 우선순위 | 태스크 | 담당 | 산출물 | 완료 기준 |
|----|----------|--------|------|--------|-----------|
| `S1-018` | P1 | 주문 생성 API (사가 패턴) | 백엔드 | handlers/order.go | Reserve → Confirm 플로우 |
| `S1-019` | P1 | 재고 예약 로직 | 백엔드 | services/stock.go | 동시성 제어 (SELECT FOR UPDATE) |
| `S1-020` | P1 | 주문 취소 API | 백엔드 | handlers/order.go | 보상 트랜잭션 |
| `S1-021` | P1 | Dockerfile 작성 (백엔드) | 인프라 | Dockerfile | 멀티스테이지 빌드 |
| `S1-022` | P1 | ECR 레포지토리 생성 | 인프라 | Terraform 코드 | 이미지 푸시 성공 |
| `S1-023` | P1 | EKS 클러스터 구축 | 인프라 | Terraform 코드 | 노드 그룹 1개, t3.medium x 2 |

#### 📝 S1-018: 주문 생성 API (사가 패턴)

```go
// POST /api/v1/orders
// Request
{
    "time_deal_id": 1,
    "quantity": 2
}

// 내부 플로우 (사가 패턴)
// Step 1: 주문 생성 (status: pending)
// Step 2: 재고 예약 (reserved_quantity += quantity)
// Step 3: 주문 상태 변경 (status: reserved)
// Step 4: (향후) 결제 처리
// Step 5: (향후) 주문 확정 (status: confirmed, sold_quantity += quantity, reserved_quantity -= quantity)

// Response 201
{
    "id": 123,
    "time_deal_id": 1,
    "quantity": 2,
    "unit_price": 29900,
    "total_price": 59800,
    "status": "reserved",
    "created_at": "2026-02-27T14:05:00Z"
}

// Error 409: 재고 부족
{
    "error": "INSUFFICIENT_STOCK",
    "message": "재고가 부족합니다",
    "available": 1,
    "requested": 2
}

// Error 400: 타임딜 비활성
{
    "error": "DEAL_NOT_ACTIVE",
    "message": "타임딜이 진행 중이 아닙니다"
}
```

#### 📝 S1-019: 재고 예약 로직 (동시성 제어)

```go
// services/stock.go

func (s *StockService) Reserve(ctx context.Context, dealID, quantity int) error {
    tx, _ := s.db.BeginTx(ctx, nil)
    defer tx.Rollback()

    // SELECT FOR UPDATE - 행 잠금
    var deal TimeDeal
    err := tx.QueryRowContext(ctx, `
        SELECT id, stock_quantity, reserved_quantity, sold_quantity, status
        FROM time_deals
        WHERE id = $1
        FOR UPDATE
    `, dealID).Scan(&deal.ID, &deal.StockQty, &deal.ReservedQty, &deal.SoldQty, &deal.Status)

    if err != nil {
        return ErrDealNotFound
    }

    if deal.Status != "active" {
        return ErrDealNotActive
    }

    available := deal.StockQty - deal.ReservedQty - deal.SoldQty
    if available < quantity {
        return ErrInsufficientStock
    }

    // 재고 예약
    _, err = tx.ExecContext(ctx, `
        UPDATE time_deals
        SET reserved_quantity = reserved_quantity + $1,
            updated_at = CURRENT_TIMESTAMP
        WHERE id = $2
    `, quantity, dealID)

    return tx.Commit()
}
```

#### 📝 S1-021: Dockerfile 명세

```dockerfile
# backend/Dockerfile

# Build stage
FROM golang:1.22-alpine AS builder
WORKDIR /app
COPY go.mod go.sum ./
RUN go mod download
COPY . .
RUN CGO_ENABLED=0 GOOS=linux go build -o main .

# Runtime stage
FROM alpine:3.19
RUN apk --no-cache add ca-certificates tzdata
WORKDIR /app
COPY --from=builder /app/main .
EXPOSE 8080
CMD ["./main"]
```

#### 📝 S1-023: EKS 명세

```hcl
# terraform/eks.tf
module "eks" {
  source  = "terraform-aws-modules/eks/aws"
  version = "19.0.0"

  cluster_name    = "bbossiregi-eks"
  cluster_version = "1.29"

  vpc_id     = module.vpc.vpc_id
  subnet_ids = module.vpc.private_subnets

  eks_managed_node_groups = {
    default = {
      name           = "default-ng"
      instance_types = ["t3.medium"]
      min_size       = 2
      max_size       = 4
      desired_size   = 2
    }
  }
}
```

---

### Day 5 (2/28 금) - 배포 및 HTTPS

| ID | 우선순위 | 태스크 | 담당 | 산출물 | 완료 기준 |
|----|----------|--------|------|--------|-----------|
| `S1-024` | P1 | K8s Deployment 매니페스트 | 인프라 | k8s/deployment.yaml | replicas: 2 |
| `S1-025` | P1 | K8s Service 매니페스트 | 인프라 | k8s/service.yaml | ClusterIP |
| `S1-026` | P1 | ALB Ingress Controller | 인프라 | k8s/ingress.yaml | ALB 자동 생성 |
| `S1-027` | P1 | ACM 인증서 발급 | 인프라 | Terraform 코드 | *.bbossiregi.com |
| `S1-028` | P2 | Route53 도메인 연결 | 인프라 | Terraform 코드 | api.bbossiregi.com |
| `S1-029` | P1 | Lambda 타임딜 스케줄러 | 인프라 | lambda/scheduler.py | EventBridge 연동 |
| `S1-030` | P0 | Sprint 1 회고 | 전체 | KPT 문서 | 팀 회고 완료 |

#### 📝 S1-024: Deployment 명세

```yaml
# k8s/backend/deployment.yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: backend
  namespace: bbossiregi
spec:
  replicas: 2
  selector:
    matchLabels:
      app: backend
  template:
    metadata:
      labels:
        app: backend
    spec:
      containers:
      - name: backend
        image: 123456789.dkr.ecr.ap-northeast-2.amazonaws.com/bbossiregi-backend:v1
        ports:
        - containerPort: 8080
        env:
        - name: DB_HOST
          valueFrom:
            secretKeyRef:
              name: db-credentials
              key: host
        - name: DB_PASSWORD
          valueFrom:
            secretKeyRef:
              name: db-credentials
              key: password
        resources:
          requests:
            cpu: "100m"
            memory: "128Mi"
          limits:
            cpu: "500m"
            memory: "512Mi"
        livenessProbe:
          httpGet:
            path: /health
            port: 8080
          initialDelaySeconds: 10
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /health
            port: 8080
          initialDelaySeconds: 5
          periodSeconds: 5
```

#### 📝 S1-029: Lambda 스케줄러 명세

```python
# lambda/scheduler.py
import boto3
import psycopg2
from datetime import datetime

def handler(event, context):
    """
    EventBridge에서 매 분마다 호출
    타임딜 상태 자동 변경
    """
    conn = get_db_connection()
    cur = conn.cursor()
    now = datetime.utcnow()
    
    # scheduled → active
    cur.execute("""
        UPDATE time_deals 
        SET status = 'active', updated_at = %s
        WHERE status = 'scheduled' 
        AND start_at <= %s AND end_at > %s
    """, (now, now, now))
    activated = cur.rowcount
    
    # active → ended
    cur.execute("""
        UPDATE time_deals 
        SET status = 'ended', updated_at = %s
        WHERE status = 'active' AND end_at <= %s
    """, (now, now))
    ended = cur.rowcount
    
    # active → soldout
    cur.execute("""
        UPDATE time_deals 
        SET status = 'soldout', updated_at = %s
        WHERE status = 'active' 
        AND stock_quantity <= reserved_quantity + sold_quantity
    """, (now,))
    soldout = cur.rowcount
    
    conn.commit()
    
    return {
        'activated': activated,
        'ended': ended,
        'soldout': soldout
    }
```

---

## 🗓️ Sprint 2: 프론트엔드 + 테스트 (Day 6-10)

### Day 6 (3/3 월) - 프론트엔드 기반

| ID | 우선순위 | 태스크 | 담당 | 산출물 | 완료 기준 |
|----|----------|--------|------|--------|-----------|
| `S2-001` | P0 | React 프로젝트 초기화 | 프론트 | package.json | Vite + React 19 |
| `S2-002` | P1 | 라우터 설정 | 프론트 | router.jsx | 5개 페이지 라우트 |
| `S2-003` | P1 | API 클라이언트 설정 | 프론트 | api/client.js | Axios + 인터셉터 |
| `S2-004` | P1 | 로그인/회원가입 페이지 | 프론트 | pages/Auth.jsx | 폼 유효성 검사 포함 |
| `S2-005` | P1 | 전역 상태 관리 설정 | 프론트 | store/auth.js | Zustand |

#### 📝 S2-002: 라우트 명세

```jsx
// src/router.jsx
const routes = [
  { path: '/', element: <Home /> },           // 메인 (타임딜 목록)
  { path: '/login', element: <Login /> },     // 로그인
  { path: '/register', element: <Register /> }, // 회원가입
  { path: '/deals/:id', element: <DealDetail /> }, // 타임딜 상세
  { path: '/orders', element: <OrderList /> },  // 내 주문 목록
  { path: '/admin', element: <AdminDashboard /> }, // 관리자 (Protected)
];
```

---

### Day 7 (3/4 화) - 타임딜 UI

| ID | 우선순위 | 태스크 | 담당 | 산출물 | 완료 기준 |
|----|----------|--------|------|--------|-----------|
| `S2-006` | P1 | 타임딜 목록 페이지 | 프론트 | pages/Home.jsx | 카드 그리드 레이아웃 |
| `S2-007` | P1 | 카운트다운 타이머 컴포넌트 | 프론트 | components/Countdown.jsx | 실시간 업데이트 |
| `S2-008` | P1 | 타임딜 상세 페이지 | 프론트 | pages/DealDetail.jsx | 재고 프로그레스바 |
| `S2-009` | P1 | 구매 버튼 컴포넌트 | 프론트 | components/BuyButton.jsx | 상태별 UI 분기 |

#### 📝 S2-007: 카운트다운 명세

```jsx
// components/Countdown.jsx
// Props: targetTime (ISO string)
// 표시 형식: "01:23:45" (시:분:초)
// 색상 규칙:
//   - 1시간 이상: 검정
//   - 10분 이하: 주황
//   - 1분 이하: 빨강 + 깜빡임
// 종료 시: "종료됨" 표시
```

#### 📝 S2-009: 구매 버튼 상태

```jsx
// 상태별 버튼 UI
{
  'scheduled': { text: '오픈 예정', disabled: true, color: 'gray' },
  'active': { text: '구매하기', disabled: false, color: 'black' },
  'soldout': { text: '품절', disabled: true, color: 'red' },
  'ended': { text: '종료됨', disabled: true, color: 'gray' },
}
```

---

### Day 8 (3/5 수) - 주문 플로우 UI

| ID | 우선순위 | 태스크 | 담당 | 산출물 | 완료 기준 |
|----|----------|--------|------|--------|-----------|
| `S2-010` | P1 | 주문 생성 플로우 | 프론트 | hooks/useOrder.js | 낙관적 UI 업데이트 |
| `S2-011` | P1 | 주문 결과 모달 | 프론트 | components/OrderModal.jsx | 성공/실패 분기 |
| `S2-012` | P1 | 내 주문 목록 페이지 | 프론트 | pages/OrderList.jsx | 상태별 필터링 |
| `S2-013` | P2 | 토스트 알림 컴포넌트 | 프론트 | components/Toast.jsx | 성공/에러/정보 |

---

### Day 9 (3/6 목) - 테스트 기반

| ID | 우선순위 | 태스크 | 담당 | 산출물 | 완료 기준 |
|----|----------|--------|------|--------|-----------|
| `S2-014` | P1 | 단위 테스트 설정 (백엔드) | 백엔드 | *_test.go | go test 실행 가능 |
| `S2-015` | P1 | 재고 예약 테스트 | 백엔드 | stock_test.go | 동시성 테스트 포함 |
| `S2-016` | P1 | API 통합 테스트 | 백엔드 | integration_test.go | 주요 플로우 커버 |
| `S2-017` | P2 | 프론트엔드 테스트 설정 | 프론트 | vitest.config.js | Vitest |

#### 📝 S2-015: 동시성 테스트 명세

```go
// services/stock_test.go

func TestConcurrentReservation(t *testing.T) {
    // Given: 재고 10개인 타임딜
    deal := createTestDeal(stock: 10)
    
    // When: 동시에 15명이 1개씩 예약 시도
    var wg sync.WaitGroup
    successCount := atomic.Int32{}
    failCount := atomic.Int32{}
    
    for i := 0; i < 15; i++ {
        wg.Add(1)
        go func() {
            defer wg.Done()
            err := stockService.Reserve(ctx, deal.ID, 1)
            if err == nil {
                successCount.Add(1)
            } else {
                failCount.Add(1)
            }
        }()
    }
    wg.Wait()
    
    // Then: 정확히 10명만 성공
    assert.Equal(t, int32(10), successCount.Load())
    assert.Equal(t, int32(5), failCount.Load())
    
    // And: 재고 정합성 유지
    deal = getDeal(deal.ID)
    assert.Equal(t, 10, deal.ReservedQuantity)
}
```

---

### Day 10 (3/7 금) - CI/CD 구축

| ID | 우선순위 | 태스크 | 담당 | 산출물 | 완료 기준 |
|----|----------|--------|------|--------|-----------|
| `S2-018` | P1 | GitHub Actions 워크플로우 | 인프라 | .github/workflows/ci.yml | 테스트 + 빌드 |
| `S2-019` | P1 | ECR 푸시 자동화 | 인프라 | .github/workflows/cd.yml | main 브랜치 머지 시 |
| `S2-020` | P1 | K8s 배포 자동화 | 인프라 | .github/workflows/cd.yml | kubectl apply |
| `S2-021` | P2 | Slack 알림 연동 | 인프라 | .github/workflows/*.yml | 성공/실패 알림 |
| `S2-022` | P0 | Sprint 2 회고 | 전체 | KPT 문서 | 팀 회고 완료 |

#### 📝 S2-018: CI 워크플로우 명세

```yaml
# .github/workflows/ci.yml
name: CI

on:
  pull_request:
    branches: [main]

jobs:
  test-backend:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
          go-version: '1.22'
      - run: cd backend && go test -v ./...
      
  test-frontend:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
      - run: cd frontend && npm ci && npm test
      
  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: golangci/golangci-lint-action@v4
        with:
          working-directory: backend
```

---

## 🗓️ Sprint 3: 모니터링 + 부하테스트 (Day 11-15)

### Day 11-12: 모니터링 스택

| ID | 우선순위 | 태스크 | 담당 | 산출물 | 완료 기준 |
|----|----------|--------|------|--------|-----------|
| `S3-001` | P1 | Prometheus 설치 | 인프라 | helm chart | 메트릭 수집 확인 |
| `S3-002` | P1 | Grafana 설치 | 인프라 | helm chart | 대시보드 접속 가능 |
| `S3-003` | P1 | 백엔드 메트릭 노출 | 백엔드 | /metrics 엔드포인트 | Prometheus 포맷 |
| `S3-004` | P1 | Grafana 대시보드 구성 | 인프라 | dashboard.json | RPS, P99, 에러율 |
| `S3-005` | P2 | CloudWatch 로그 연동 | 인프라 | Fluent Bit | 로그 중앙화 |

#### 📝 S3-003: 백엔드 메트릭 명세

```go
// 노출할 메트릭
http_requests_total{method, path, status}     // 요청 수
http_request_duration_seconds{method, path}   // 응답 시간
stock_reservation_total{deal_id, result}      // 재고 예약 수
active_deals_count                            // 활성 타임딜 수
```

---

### Day 13-14: 부하 테스트

| ID | 우선순위 | 태스크 | 담당 | 산출물 | 완료 기준 |
|----|----------|--------|------|--------|-----------|
| `S3-006` | P1 | k6 테스트 스크립트 작성 | 전체 | k6/load-test.js | 시나리오 3개 |
| `S3-007` | P1 | 부하 테스트 실행 | 전체 | 결과 리포트 | 1000 VU 테스트 |
| `S3-008` | P1 | 병목 지점 분석 | 전체 | 분석 문서 | 개선 포인트 도출 |
| `S3-009` | P1 | 성능 최적화 적용 | 백엔드 | 코드 수정 | P99 < 500ms |

#### 📝 S3-006: k6 테스트 시나리오

```javascript
// k6/load-test.js

// 시나리오 1: 타임딜 조회 부하
export function browsing() {
  http.get(`${BASE_URL}/api/v1/timedeals`);
  sleep(1);
}

// 시나리오 2: 동시 구매 부하 (타임딜 오픈 시뮬레이션)
export function purchase() {
  const res = http.post(`${BASE_URL}/api/v1/orders`, JSON.stringify({
    time_deal_id: 1,
    quantity: 1
  }), { headers: { 'Content-Type': 'application/json' } });
  
  check(res, {
    'status is 201 or 409': (r) => r.status === 201 || r.status === 409,
  });
}

// 시나리오 3: 혼합 부하
export const options = {
  scenarios: {
    browsing: {
      executor: 'constant-vus',
      vus: 100,
      duration: '5m',
      exec: 'browsing',
    },
    purchase: {
      executor: 'ramping-vus',
      startVUs: 0,
      stages: [
        { duration: '30s', target: 500 },   // 30초간 500명까지 증가
        { duration: '1m', target: 1000 },   // 1분간 1000명까지 증가
        { duration: '30s', target: 0 },     // 30초간 감소
      ],
      exec: 'purchase',
    },
  },
};
```

---

### Day 15: HPA + 스케일링 테스트

| ID | 우선순위 | 태스크 | 담당 | 산출물 | 완료 기준 |
|----|----------|--------|------|--------|-----------|
| `S3-010` | P1 | HPA 설정 | 인프라 | k8s/hpa.yaml | CPU 70% 기준 |
| `S3-011` | P1 | 스케일링 테스트 | 인프라 | 테스트 결과 | 30초 내 스케일 아웃 |
| `S3-012` | P2 | PDB 설정 | 인프라 | k8s/pdb.yaml | minAvailable: 1 |
| `S3-013` | P0 | Sprint 3 회고 + 중간 발표 준비 | 전체 | 발표 자료 | 3/9 중간 발표 |

#### 📝 S3-010: HPA 명세

```yaml
# k8s/hpa.yaml
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: backend-hpa
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: backend
  minReplicas: 2
  maxReplicas: 10
  metrics:
  - type: Resource
    resource:
      name: cpu
      target:
        type: Utilization
        averageUtilization: 70
  behavior:
    scaleUp:
      stabilizationWindowSeconds: 30
      policies:
      - type: Pods
        value: 4
        periodSeconds: 60
```

---

## 🗓️ Sprint 4: 마무리 + 발표 (Day 16-20)

### Day 16-17: 문서화

| ID | 우선순위 | 태스크 | 담당 | 산출물 | 완료 기준 |
|----|----------|--------|------|--------|-----------|
| `S4-001` | P0 | 아키텍처 문서 최종화 | 전체 | ADR 문서 | 의사결정 기록 |
| `S4-002` | P0 | API 문서 완성 | 백엔드 | Swagger UI | 전체 API 문서화 |
| `S4-003` | P0 | 운영 가이드 작성 | 인프라 | RUNBOOK.md | 장애 대응 절차 |
| `S4-004` | P1 | README 완성 | 전체 | README.md | 설치/실행 가이드 |

---

### Day 18-19: 발표 준비

| ID | 우선순위 | 태스크 | 담당 | 산출물 | 완료 기준 |
|----|----------|--------|------|--------|-----------|
| `S4-005` | P0 | 결과 보고서 작성 | 전체 | 보고서.pdf | 템플릿 준수 |
| `S4-006` | P0 | 발표 PPT 작성 | 전체 | 발표.pptx | 15분 분량 |
| `S4-007` | P0 | 시연 영상 제작 | 전체 | demo.mp4 | 3-5분 |
| `S4-008` | P1 | 발표 리허설 | 전체 | - | 2회 이상 |

---

### Day 20 (3/19): 최종 발표

| ID | 우선순위 | 태스크 | 담당 | 산출물 | 완료 기준 |
|----|----------|--------|------|--------|-----------|
| `S4-009` | P0 | 최종 발표 | 전체 | - | 15분 발표 완료 |
| `S4-010` | P0 | 결과물 제출 | 팀장 | 구글폼 | 13:00까지 |
| `S4-011` | P0 | 프로젝트 회고 | 전체 | KPT 문서 | 최종 회고 |

---

## 📊 Definition of Done (DoD)

### 코드
- [ ] PR 생성 및 코드 리뷰 완료
- [ ] 테스트 통과 (커버리지 70% 이상)
- [ ] lint 에러 없음
- [ ] main 브랜치 머지 완료

### 인프라
- [ ] Terraform plan 성공
- [ ] 리소스 생성 완료
- [ ] 헬스체크 통과
- [ ] 모니터링 대시보드에 표시

### 문서
- [ ] 노션 페이지 업데이트
- [ ] ADR 작성 (의사결정 사항)
- [ ] README 반영

---
